---
# Déploie la stack WordPress (mysql + wordpress-fpm + phpmyadmin + nginx) via docker compose.

- name: Create project directories
  file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
  loop:
    - "{{ project_dir }}"
    - "{{ project_dir }}/mysql"
    - "{{ project_dir }}/wp"
    - "{{ project_dir }}/nginx"
    - "{{ project_dir }}/ssl"

- name: Generate self-signed SSL certificate
  command: >
    openssl req -x509 -nodes -days 365 -newkey rsa:2048
    -keyout {{ project_dir }}/ssl/key.pem
    -out {{ project_dir }}/ssl/cert.pem
    -subj "/C=FR/ST=Paris/L=Paris/O=42/CN={{ domain_name }}"
  args:
    creates: "{{ project_dir }}/ssl/cert.pem"

- name: Deploy nginx config
  template:
    src: nginx.conf.j2
    dest: "{{ project_dir }}/nginx/nginx.conf"
    mode: '0644'
  notify: Restart stack

- name: Deploy .env file (secrets)
  template:
    src: env.j2
    dest: "{{ project_dir }}/.env"
    mode: '0600'
  notify: Restart stack

- name: Deploy docker-compose.yml
  template:
    src: docker-compose.yml.j2
    dest: "{{ project_dir }}/docker-compose.yml"
    mode: '0644'
  notify: Restart stack

- name: Start the stack
  command: docker compose up -d --remove-orphans
  args:
    chdir: "{{ project_dir }}"
  register: compose_up
  changed_when: >
    ('Started' in compose_up.stderr) or ('Created' in compose_up.stderr)
    or ('Recreated' in compose_up.stderr)
