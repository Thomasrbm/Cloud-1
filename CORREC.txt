Depuis ~/Cloud-1. Mot de passe vault : Cloud1Vault2026

LANCER
  ansible-playbook playbook.yml --ask-vault-pass
  -> https://13.60.218.238/  et  https://13.60.218.238/phpmyadmin/

SUPPRIMER
  ansible-playbook teardown.yml                    (stack + donnees)
  ansible-playbook teardown.yml -e full_wipe=true  (+ Docker et ufw : Ubuntu nu)

MACHINE NEUVE (sinon rien a faire)
  git clone https://github.com/Thomasrbm/Cloud-1.git
  cd Cloud-1
  sudo apt install -y ansible
  ansible-galaxy collection install -r requirements.yml
  cp <cle_usb>/webservers.yml group_vars/webservers.yml   # pas dans git : secrets

SI L'IP A CHANGE
  sed -i 's|13.60.218.238|<NOUVELLE_IP>|' inventory.ini
