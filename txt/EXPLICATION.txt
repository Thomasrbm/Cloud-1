


1/  port 80 et 443  +  databse juste acc sur les ports necessaire ? 

nmap -Pn 13.60.218.238
Vu de l'extérieur, seuls 22/80/443 sont ouverts 
et 3306 absents.



22 est indispensable pour qu'Ansible déploie
et pour l'accès root exigé


==> car cest le port ssh et ansible ne parle que par ca 








==============================



2.0/ ansible deployement script  code from machin to vps
exec script 2 endroits


modifier dans le inventory.ini

ansible-playbook playbook.yml --limit server2 --skip-tags ssh,ufw --ask-vault-pass



+

decommenter  dans rp;es/proxy/template/nginx.conf

aller sur  https://13.60.218.238/preuve




2.1/  idempotency ? 



same 





2.2/   plusieurs role ?  capable deploy indep


down phpmyadmin =>
ansible webservers -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose rm -sf phpmyadmin"


ansible-playbook playbook.yml --tags phpmyadmin --ask-vault-pass





2.3/ pas de hard code secrets (mdp db, ou dans le code)



 sudo grep -rn "password" --include='*.yml' --include='*.j2' --include='*.cfg' . | grep -v vault 


montrer le crypted => 

head -3 group_vars/webservers.yml

ansible-vault view group_vars/webservers.yml



.env protected  =>

ansible server1 -b --ask-vault-pass -a "ls -l /opt/wordpress/.env"

==============================


3/ docker compose a cache? 

on construit aucune image , on prend que des offi 
legeres =>


find . -name "Dockerfile*"

ansible server1 -b --ask-vault-pass -a "docker images"


pas de build:  que des image: 


si avait image : 


COPY . /app          ← change à chaque commit
RUN apt install php  ← rebuild à chaque fois, 2 min perdues
Bon ordre

RUN apt install php  ← en cache, jamais rejoué
COPY . /app          ← seule couche reconstruite








3.1/ container separed ? un pour db, php run , web proxy
ils communique ensemble ? 



ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose ps"





ansible server1 -b --ask-vault-pass -a "docker network inspect wordpress_wp_network" | grep -E '"Name"|Driver|Subnet'

====> meme bridge 












3.2/ data stored ans volume mouted ?  on peut voir en admin ? 



ansible server1 -b --ask-vault-pass -a "docker volume ls"



ou sont stock : 
ansible server1 -b --ask-vault-pass -a "docker volume inspect wordpress_wp_data"




seul admin peut voir ? 


ansible server1 -b --ask-vault-pass -a "ls -ld /var/lib/docker /var/lib/docker/volumes"

drwx--x--- => seul root









3.3/ on peut launch tous les container d un cuop ?  les restart individualy ? 


tous down :


ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose down"


tous up : 

ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose up -d"




un par un : 



mysql
ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose stop mysql"
ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose start mysql"



wordpress
ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose stop wordpress"
ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose start wordpress"



phpmyadmin
ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose stop phpmyadmin"
ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose start phpmyadmin"



nginx
ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose stop nginx"
ansible server1 -b --ask-vault-pass -a "chdir=/opt/wordpress docker compose start nginx"
